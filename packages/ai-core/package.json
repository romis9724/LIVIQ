{
  "name": "@liviq/ai-core",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --no-sync ruff check .",
    "typecheck": "uv run --no-sync mypy src tests",
    "test": "uv run --no-sync pytest"
  }
}
