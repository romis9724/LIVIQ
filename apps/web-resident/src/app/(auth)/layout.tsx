import type { ReactNode } from "react";

/** 온보딩 레이아웃 — 입주민 하단 탭 셸 밖. 페이지가 각자 중앙 정렬(.auth-shell)한다. */
export default function AuthLayout({ children }: { children: ReactNode }) {
  return children;
}
